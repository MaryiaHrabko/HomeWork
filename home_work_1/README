1. Побитовые операции:
    BitOperators1 >> 1.2 Использовать ВСЕ возможные побитовые операции с числами 42 и 15.
    BitOperators2 >> 1.3 Использовать ВСЕ возможные побитовые операции с числами -42 и -15.
    BitOperators3 >> 1.5 Попробуйте сделать побитовую операцию с числова 42.5.

2. Приоритетность операторов:
    Priority1 >> 2.1 5 + 2 / 8
    Priority2 >> 2.2 (5 + 2) / 8
    Priority3 >> 2.3 (5 + 2++) / 8
    Priority4 >> 2.4 (5 + 2++) / --8
    Priority5 >> 2.5 (5 * 2 >> 2++) / --8
    Priority6 >> 2.6 (5 + 7 > 20 ? 68 : 22 * 2 >> 2++) / --8
    Priority7 >> 2.7 (5 + 7 > 20 ? 68 >= 68 : 22 * 2 >> 2++) / --8
    Priority8 >> 2.8 6 - 2 > 3 && 12 * 12 <= 119
    Priority9 >> 2.9 true && false

3. Библиотека (класс) Math
   Mathematics

4. Создать СТАТИЧЕСКИЙ метод sleepIn рядом с методом main. (Взято с https://codingbat.com/prob/p187868).
   SleepIn

5. Операторы ветвления. Запросить у пользователя его имя. В данном задании у вас должно получиться 3 класса.
    Loops1 >> 5.1 Написать в отдельном классе в отдельном main код который будет выполнять общее условие при помощи if
    Loops2 >> 5.2 Написать в отдельном классе в отдельном main код который будет выполнять общее условие при помощи if else if
    Loops3 >> 5.3 Написать в отдельном классе в отдельном main код который будет выполнять общее условие при помощи switch

6. В метод будет передаваться массив из 10 цифр (от 0 до 9). Данные цифры должны быть помещены в строку формата: (XXX) XXX-XXXX.
   PhoneNumber

7. Создать СТАТИЧЕСКИЙ метод String toBinaryString(byte number)
   BinaryString

8. Переделать задание 5.
	8.1 Создать интерфейс ICommunicationPrinter. В данном интерфейсе должен быть объявлен метод String welcom(String name)
	8.2 Классы созданные ранее должны реализовывать интерфейс ICommunicationPrinter выполняя поставленную задачу в задании 5 и более не иметь точки входа внутри себя.
	Interface1

	